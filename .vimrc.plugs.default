" Default vim-plugs {

    " Use before config if available {
        if filereadable(expand("~/.vimrc.before"))
            source ~/.vimrc.before
        endif
    " }

    " Use vim-plugs config {
        if filereadable(expand("~/.vimrc.plugs"))
            source ~/.vimrc.bundles
        endif
    " }
" }
